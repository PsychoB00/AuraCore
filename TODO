documantation:
    - [] Create some docs for establishing some standards (routing, URI, API, time formating, units, etc.)

testing:
    - [] Add at least some basic testing

context:
    - [] Add tools Context checking

utils:
    - [] Add config struct (for passing Allocator, Io, Timezone, env, etc.)
    - [] Fix namespacing

log:
    - [] Put Options as last comptime argument
    - [] Fix namespacing
    - [] Add comptime validation
    - [] Check if ConsoleLogProcessor rework for zig 0.15.1 is needed
    - [] Check for formating issues (guard conditions, std namespace reduction)

jwt:
    - [] Fix namespacing
    - [] Fix long lines

json:
    - [] Add comments
    - [] Add enum support
    - [] Add asserts
    - [] Make comptime json type validator

routing:
    - [] Add asserts
    - [] Fix namespacing
    - ResourceTree:
        - [] Rework it to individual resource Options
        - [] Fix comptime validation, too much nesting, try using guard conditions etc.
        - [] Fix namespacing
    - Router:
        - [] Rework for ResourceTree rework
        - [] Fix for StaticResource rework
        - [] Add unauthorized function
        - [] Add OnRequestProcessor
        - [] Reduce nesting and fix namespacing
    - resources:
        - [] Change StaticResource data from comptime embeded file to runtime read file
        - [] Fix StaticResource namespacing
        - [] Add proper APIResouce for ResourceTree rework
        - [] Add comptime MIME types to BodyParameters
        - [] Fix comptime validation of BodyParameters
        - [] Fix APIResource namespacing
